<div class="container">
  <div class="row justify-content-around mt-5">
      <% @flats.each do |flat| %>
        <div class="col-4 p-3">
          <div class="card p-3">
            <h3><%= link_to flat.name, flat_path(flat) %></h3>
            <p><%= flat.address %></p>
            <p>👥<%= flat.number_of_guests %>  € <%= flat.price_per_night %></p>
          <p>
            <%= link_to edit_flat_path(flat) do %>
              ✎
            <% end %>
            <%= link_to flat_path(flat), method: :delete, class: "ml-3" do %>
              🗑
            <% end %>
          </p>
          </div>
        </div>
    <% end %>
  </div>
</div>
